// Return result from failures you can recover from

// An example can be a user enters the wrong type of input
fn expect_number() {
    let user_choice = "a";
    // add_number_to_list(user_choice)
}